{
  "id": "postgresql-reference",
  "title": "PostgreSQL",
  "slug": "postgres",
  "category": "Databases",
  "technology": "PostgreSQL",
  "version": "PostgreSQL 18",
  "difficulty": "INTERMEDIATE",
  "readTimeMinutes": 24,
  "description": "PostgreSQL reference covering schema design, types, constraints, queries, indexes, transactions, MVCC, locking, JSONB, full-text search, partitioning, security, backup, replication, tuning, and diagnostics.",
  "tags": [
    "POSTGRESQL",
    "DATABASE",
    "SQL",
    "MVCC",
    "INDEXING"
  ],
  "updatedAt": "Jul 2026",
  "aliases": [
    "postgresql",
    "psql",
    "pg",
    "relational database"
  ],
  "relatedSheets": [
    "sql",
    "redis",
    "fastapi",
    "docker",
    "aws",
    "elasticsearch",
    "system-design"
  ],
  "prerequisites": [
    {
      "title": "SQL Language",
      "slug": "sql"
    }
  ],
  "githubEditUrl": "https://github.com/elnoxvie/carbonsheet/edit/main/sheets/postgres.json",
  "sections": [
    {
      "id": "schema-types",
      "title": "Schema, Types & Constraints",
      "subtopics": [
        {
          "id": "types-keys-constraints",
          "title": "Types, Keys & Integrity",
          "description": "Use the narrowest semantic type and encode invariants with primary, foreign, unique, not-null, exclusion, and check constraints.",
          "table": {
            "title": "Common PostgreSQL Type Selection",
            "headers": [
              "Need",
              "Type",
              "Note"
            ],
            "rows": [
              [
                "Identifier",
                "bigint generated / uuid",
                "Choose distribution and index tradeoff"
              ],
              [
                "Exact decimal",
                "numeric(p,s)",
                "Define precision/rounding policy"
              ],
              [
                "Timestamp instant",
                "timestamptz",
                "Store instant; render in user zone"
              ],
              [
                "Local civil time",
                "timestamp",
                "No zone conversion"
              ],
              [
                "Flexible indexed document",
                "jsonb",
                "Keep core relational fields relational"
              ],
              [
                "Finite state",
                "text + CHECK / enum",
                "Migration tradeoffs differ"
              ]
            ]
          },
          "codeBlocks": [
            {
              "language": "sql",
              "code": "CREATE TABLE orders (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  customer_id bigint NOT NULL REFERENCES customers(id),\n  status text NOT NULL CHECK (status IN ('pending','paid','cancelled')),\n  total numeric(12,2) NOT NULL CHECK (total >= 0),\n  created_at timestamptz NOT NULL DEFAULT now()\n);"
            }
          ]
        },
        {
          "id": "normalization-jsonb",
          "title": "Normalization, Arrays & JSONB",
          "description": "Normalize authoritative entities and relationships; use arrays/JSONB for bounded document attributes whose access and integrity requirements fit.",
          "callouts": [
            {
              "title": "JSONB Is Not a Free Schema Escape",
              "type": "warning",
              "icon": "⚠️",
              "text": "Frequent joins, uniqueness, foreign keys, and field updates usually belong in columns/tables. Document JSON shape and index only queried paths.",
              "color": "amber"
            }
          ]
        },
        {
          "id": "partitioning-generated-identity",
          "title": "Partitioning, Generated Values & UUIDs",
          "description": "Partition only when pruning, retention, or maintenance isolation justifies the operational cost. Prefer identity columns for generated numeric keys and use database-supported UUID generation appropriate to ordering and distribution needs.",
          "codeBlocks": [
            {
              "language": "sql",
              "code": "CREATE TABLE events (\n  id uuid PRIMARY KEY,\n  occurred_at timestamptz NOT NULL,\n  payload jsonb NOT NULL\n) PARTITION BY RANGE (occurred_at);\n\nCREATE TABLE events_2026_07 PARTITION OF events\nFOR VALUES FROM ('2026-07-01') TO ('2026-08-01');"
            }
          ],
          "callouts": [
            {
              "title": "Partitioning Is Not Automatic Speed",
              "type": "warning",
              "icon": "⚠️",
              "text": "It adds routing, constraint, index, vacuum, backup, and migration complexity. Verify pruning in plans and automate partition creation and retirement before adopting it.",
              "color": "amber"
            }
          ],
          "tags": [
            "partitioning",
            "identity",
            "uuid"
          ]
        }
      ]
    },
    {
      "id": "queries-indexes",
      "title": "Queries, Plans & Indexes",
      "subtopics": [
        {
          "id": "joins-windows-cte",
          "title": "Joins, Aggregates, Windows & CTEs",
          "description": "Use joins for relationships, GROUP BY for row reduction, window functions for analytics without collapse, and CTEs for clarity or explicit materialization.",
          "codeBlocks": [
            {
              "language": "sql",
              "code": "SELECT customer_id, created_at, total,\n       sum(total) OVER (PARTITION BY customer_id ORDER BY created_at) AS running_total\nFROM orders\nWHERE status = 'paid';"
            }
          ]
        },
        {
          "id": "index-selection-explain",
          "title": "B-tree, GIN, GiST, BRIN & EXPLAIN",
          "description": "Index predicates, joins, ordering, and constraints from real workloads. Column order, selectivity, correlation, and write cost determine usefulness.",
          "codeBlocks": [
            {
              "language": "sql",
              "code": "CREATE INDEX orders_customer_created_idx ON orders (customer_id, created_at DESC) INCLUDE (total) WHERE status = 'paid';\nEXPLAIN (ANALYZE, BUFFERS) SELECT ...;"
            }
          ],
          "callouts": [
            {
              "title": "EXPLAIN ANALYZE Executes the Query",
              "type": "danger",
              "icon": "🛑",
              "text": "Use care with writes and expensive production queries. Start with EXPLAIN and reproduce safely when possible.",
              "color": "red"
            }
          ]
        }
      ]
    },
    {
      "id": "transactions-concurrency",
      "title": "Transactions, MVCC & Locking",
      "subtopics": [
        {
          "id": "isolation-locks",
          "title": "Isolation Levels, Row Locks & Deadlocks",
          "description": "MVCC gives statement snapshots while writes still lock. Pick isolation from anomalies, lock rows in consistent order, and retry serialization/deadlock failures.",
          "table": {
            "title": "PostgreSQL Isolation",
            "headers": [
              "Level",
              "Snapshot",
              "Use"
            ],
            "rows": [
              [
                "Read Committed",
                "Each statement",
                "Default transactional CRUD"
              ],
              [
                "Repeatable Read",
                "Transaction snapshot",
                "Stable multi-query view; retry serialization failures"
              ],
              [
                "Serializable",
                "Serializable outcome or failure",
                "Strong invariant workflows with retry"
              ]
            ]
          },
          "codeBlocks": [
            {
              "language": "sql",
              "code": "BEGIN;\nSELECT balance FROM accounts WHERE id = 42 FOR UPDATE;\nUPDATE accounts SET balance = balance - 100 WHERE id = 42;\nCOMMIT;"
            }
          ]
        },
        {
          "id": "upsert-queues-pooling",
          "title": "UPSERT, SKIP LOCKED & Connection Pools",
          "description": "Use ON CONFLICT for atomic insert/update, SKIP LOCKED for competing database workers, and bounded pools sized to database capacity.",
          "codeBlocks": [
            {
              "language": "sql",
              "code": "INSERT INTO counters(key, value) VALUES ('orders', 1)\nON CONFLICT (key) DO UPDATE SET value = counters.value + 1\nRETURNING value;"
            }
          ],
          "callouts": [
            {
              "title": "More Connections Can Reduce Throughput",
              "type": "warning",
              "icon": "⚠️",
              "text": "Oversized pools increase contention and memory. Budget connections across replicas, jobs, migrations, admin, and failover.",
              "color": "amber"
            }
          ]
        }
      ]
    },
    {
      "id": "operations-security",
      "title": "Operations, Security & Production",
      "subtopics": [
        {
          "id": "vacuum-backup-replication",
          "title": "VACUUM, Backup, PITR & Replication",
          "description": "Autovacuum reclaims dead tuples and protects transaction IDs; base backups plus archived WAL enable point-in-time recovery; streaming and logical replication serve different topology and migration needs.",
          "codeBlocks": [
            {
              "language": "sql",
              "code": "SELECT * FROM pg_stat_activity WHERE state <> 'idle';\nSELECT relname, n_live_tup, n_dead_tup FROM pg_stat_user_tables ORDER BY n_dead_tup DESC;\nSELECT application_name, state, replay_lag FROM pg_stat_replication;"
            }
          ],
          "callouts": [
            {
              "title": "A Backup Exists Only After Restore Testing",
              "type": "danger",
              "icon": "🛟",
              "text": "Continuously verify base backups, WAL retention, encryption keys, recovery targets, extensions, roles, and application connectivity in an isolated restore drill.",
              "color": "red"
            }
          ]
        },
        {
          "id": "postgres-18-upgrade",
          "title": "PostgreSQL 18 Upgrade Notes",
          "description": "PostgreSQL 18 introduces a new asynchronous I/O subsystem, broader index use, faster major upgrades, page checksums by default for new clusters, and optional protocol 3.2 negotiation.",
          "codeBlocks": [
            {
              "language": "bash",
              "code": "pg_upgrade --check <required-options>\n# Inventory extensions, collations, checksums, client drivers and poolers\n# Benchmark representative queries and rehearse rollback before cutover"
            }
          ],
          "callouts": [
            {
              "title": "Check Client and Storage Compatibility",
              "type": "warning",
              "icon": "⚠️",
              "text": "Review drivers, proxies, poolers, extensions, checksums, collation changes, replicas, backup tooling, and statistics warm-up. Upgrade behavior depends on the source cluster and chosen migration method.",
              "color": "amber"
            }
          ],
          "tags": [
            "postgresql 18",
            "upgrade",
            "aio",
            "checksums"
          ]
        },
        {
          "id": "production-checklist",
          "title": "Security, Monitoring & Checklist",
          "description": "Use roles, least privilege, TLS, parameterized queries, statement/lock timeouts, query statistics, capacity alarms, and tested recovery.",
          "callouts": [
            {
              "title": "Production Checklist",
              "type": "tip",
              "icon": "✅",
              "text": "Enforce constraints, index from plans, keep transactions short, bound pools, set timeouts, monitor locks/bloat/replication lag, test backups, version migrations, and use expand-contract deploys.",
              "color": "green"
            }
          ]
        }
      ]
    }
  ]
}
